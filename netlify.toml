[build]
  command = "npm run build"
  publish = "out"

# Handle Single-Page Application routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Fallback for 404 errors
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
